name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version'
        required: true
        type: string
      deps-tag:
        description: 'Deps tag'
        required: true
        type: string
        default: 'build-deps'
